- Questions
  - should we test allocating memory? (ringbuffer.c line 10). if no how to
      pass it in coverage?
  - does rb_deinit need to be tested?
  - what number EXIT_FAILURE returns? at first it was 8 and my tests passed
  -   but when I was impementing another functions it was returning 1.
